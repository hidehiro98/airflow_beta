<p>
  <% if locale == I18n.default_locale %>
    <%= link_to t('.edited'), request_url(@receiver.request, subdomain: 'www', locale: nil) %>
  <% else %>
    <%= link_to t('.edited'), request_url(@receiver.request, subdomain: 'www', locale: locale) %>
  <% end %>
</p>

<p>
  <%= "#{t('.title')}: #{@receiver.request.title}" %>
</p>
<p>
  <%= "#{t('.content')}: #{@receiver.request.content}" %>
</p>
